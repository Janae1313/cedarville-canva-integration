# Example environment variables for the Cedarville–Canva Integration
#
# Copy this file to `.env` and fill in the values before running the server.
#
# The Canva Client ID and Client Secret are obtained from the Canva
# Developer Portal when you register your integration.  See the project
# README for guidance.

CANVA_CLIENT_ID=your-canva-client-id
CANVA_CLIENT_SECRET=your-canva-client-secret

# The base URL where your server is hosted.  In development this can
# be http://127.0.0.1:3000.  In production it must be a public HTTPS
# address that is registered as a redirect URI in the Canva Developer Portal.
BASE_URL=http://127.0.0.1:3000

# The redirect URI Canva will call back to after the user authorizes
# your app.  This must match one of the redirect URLs configured in
# the Canva Developer Portal.
REDIRECT_URI=http://127.0.0.1:3000/oauth/callback

# Secret key used to sign session cookies.  Use a long, random string in
# production to secure session data.
SESSION_SECRET=change-this-session-secret

# Optionally set the PORT if you don't want to use the default 3000
#PORT=3000
